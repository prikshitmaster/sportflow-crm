node_modules
dist
dist-electron
.env
.env.*
.env.local
.env.*.local
*.local
.DS_Store
Thumbs.db
.crm_backup.spl
crm_backup.sql
supabase/.temp/
playwright-report/
test-results/
playwright.config.js

.supabase-pat
.supabase-db-url

# Local Claude Code settings (machine-specific; may contain secrets)
.claude/settings.local.json

.vercel

# Android release signing (never commit — losing these means never
# updating the Play Store listing under this key again)
android/*.keystore
android/*.jks
android/keystore.properties
